# 跨会话交接文档 PRJ-001（最终版）

> 项目：feishu-robot-project 飞书机器人（feishu-robot-project）
> 版本：v2.7　生成日期：2026-08-18
> 状态：**全生命周期完成（M1-M7 全部闭环）+ 真实消息验收通过 + CMDB 冲突消解 + OpenClaw 主链路恢复 + 废弃 webhook 桥彻底移除**

---

## 1. 工作断点区（新会话第一步必读）

### 1.1 当前阶段与进度
- **当前阶段**：归档交接（M7）已完成 ✅ 项目完工，真实消息验收已通过
- **已完成**：项目启动（M1）→ 需求分析（M2）→ 架构设计（M3）→ 开发编码（M4）→ 测试验收（M5）→ 投产部署（M6）→ 归档交接（M7）
- **新增（2026-08-16）**：CMDB 环境冲突消解——ai-assistant-system file_bot 5102→5082；CMDB 补登端口归属（5102/5103→本项目）；CMDB CLI `--force` UPDATE 缺陷已修复并实测验证；全程留痕 OP-003
- **服务状态**：test 环境正式投产，稳定运行（health ok，0 ERROR）
- **GitHub**：https://github.com/gogojaja/feishu-robot-project（public，历史已脱敏）

### 1.2 环境与运行状态
- **主链路（ADR-001，2026-08-18 校准，详见 `架构事实基线.md`）**：OpenClaw Gateway（18789，feishu 长连接 WebSocket）→ acpx → opencode ACP，免 ngrok。真实入站已验证（2026-08-18 23:26-23:27 三条消息 dispatch）。
- **废弃 webhook 桥（2026-08-18 完成彻底移除）**：自研 Flask Webhook（5103）+ opencode serve（5102）+ ngrok 已全部停止并移出主树，归档于 `备份/废弃_webhook桥_20260818/`。`config/` 已清理。`check_env.py` 新增废弃残留阻断，任何复现（废弃代码/5102/5103/ngrok）会红灯告警。
- **CMDB 端口归属**（2026-08-17 二次复核校准，详见 §1.4 待办）：
  - 5101(callback)/5082(file_bot)/5081(code_api)/8080(llama)/11434(ollama) → ai-assistant-system（`/Volumes/BR256G/ai-assistant-system`，test，callback 5101 运行中）；注：`/Users/gogo/ai-assistant-system` 为旧副本（配置仍写 5001/5002/5003），非运行实例，待负责人清理
  - 5080 → free-api-hub 已删除且孤儿进程清理释放 ✅（现空闲）
  - 4040 → 现空闲（飞书 ngrok 回调已随废弃 webhook 桥移除）✅
  - DevProjectTeamSkill（`/Volumes/BR256G/DevProjectTeamSkill`）：config 无 port 字段，未监听端口（11434 实为 ai-assistant-system ollama 配置端口）
  - openclaw（`/Volumes/KINGSTON120G/openclaw`）：空脚手架（configs/src 为空），未运行、无端口，已补登
  - **环境治理台账**：`台账/25_环境资源清单.csv`（2026-08-17 建立，多项目全局资源注册表，主机 `gogojajadeMac-mini.local`，先注册先得 + 冲突留痕；替代文本型 CMDB 归属表，为权威清单）
  - **资源申请门禁（先注册后使用）**：任何独占资源（端口/容器/目录/模型/数据库等）使用前必须先在 `25_环境资源清单.csv` 注册；校验命令 `bash scripts/check_env_resource.sh <type> <identifier> [project]`（type∈端口|容器|模型|目录|数据库|缓存|GPU|域名|Docker运行时）——冲突(异归属已占用)返回 BLOCK 阻断，未注册返回 OK 可注册，同归属已占用返回 OK，空闲/释放/待清理返回 WARN。新项目接入须补登后再启动服务。
  - **提交级强制门禁**：`scripts/pre-commit-env-gate.sh` 校验 `25_清单` 无「同端口被不同项目以 已占用 重复登记」冲突，冲突即阻断 git commit。安装（随仓库分发，克隆后一键启用）：`bash scripts/setup-hooks.sh`（将钩子安装至 `.git/hooks/pre-commit`，无需手敲 cp）。
- 主链路健康检查：`openclaw channels status`（feishu 应 running）；`python3 scripts/check_env.py` 全绿为健康
- 环境标记：`.env_type=test`

### 1.3 交接资料清单
- 台账：`台账/` 17 CSV（收尾归档已更新，17_收尾归档.csv）
- 源码：OpenClaw 网关承载（`~/.openclaw/openclaw.json`），本项目无自研桥接源码（废弃 webhook 桥已归档至 `备份/废弃_webhook桥_20260818/`）
- 需求：`requirements/`　架构：`架构资产/`（31 份）　开发：`开发资产/`　测试：`测试资产/`（4 份）　投产：`投产资产/`（6 目录）
- 运维：`投产资产/06_运维交接/运维手册.md` + `已知问题清单.csv`
- 备份：`备份/`（M4/M5/M6/M7_20260809 + 废弃_webhook桥_20260818 + git_rewrite bundle + skills v21.0.0）
- 远程：origin → https://github.com/gogojaja/feishu-robot-project.git

### 1.4 已知问题与风险
- KNOWN-001：真实 open_id 推送未验证 → **已闭环**（2026-08-09 真实消息全链路验收通过）
- KNOWN-002：P95 性能基准未测 → **已闭环**（补测 P95=13.2s excl warmup，报告 v0.2，基准脚本已随 webhook 桥归档）
- KNOWN-003：Flask 开发服务器（非生产 WSGI）→ **已闭环**（2026-08-18 webhook 桥彻底废弃，主链路为 OpenClaw）
- **安全**：真实 app_secret 仅在 `~/.openclaw/openclaw.json`（本地，不推送仓库）；当前应用 cli_aa82c1b457b89bc3，长连接来源校验由 OpenClaw feishu 渠道承载
- **废弃闭环（2026-08-18）**：自研 Flask webhook（5103）+ opencode serve（5102）+ ngrok + launchd 守护已全部停止、移出 git 主树，归档 `备份/废弃_webhook桥_20260818/`；`config/` 已清空；`check_env.py` 与 `pre-commit-env-gate.sh` 新增废弃残留阻断，杜绝复现
- **治理待办（2026-08-17 二次复核校准）**：① 原 §1.2 CMDB 归属表已据「磁盘目录+config+实际监听」三源二次校准（见上）；② ai-assistant-system 真实运行实例在 `/Volumes/BR256G/ai-assistant-system`（端口 5101/5082/5081/8080/11434，无禁用 500x），`/Users/gogo/ai-assistant-system` 为旧副本待负责人清理；③ free-api-hub 已删除且 PID 71915 占 5080 已清理 ✅；④ openclaw 已补登 CMDB（空脚手架，无端口）；⑤ DevProjectTeamSkill 4040 实为飞书 ngrok、11434 实为 ai-assistant-system ollama 端口，原归属记录已校正
- **遗留交接（2026-08-19 登记，2026-08-19 22:22 已闭环 ✅）**：飞书回复流式卡片创建 HTTP 400 → 根因精确定位：`cardkit:card:write` 仅授权为 user 身份（scope_type=user），未授权 tenant 身份，OpenClaw 用 tenant_access_token 调用即报 99991672。处置：飞书开放平台权限管理将 cardkit:card:write 加入 tenant 数组 → 发布新版本 → 重启 Gateway 验证通过（`Started streaming` + `Closed streaming` 全程成功，0 ERROR）。完整证据链见 `投产资产/06_运维交接/排查交接_流式卡片400_20260819.md`
- **环境治理闭环（2026-08-17 建立，全部已 git 固化）**：`台账/25_环境资源清单.csv`（全局资源注册表，主机 gogojajadeMac-mini.local）＋ `scripts/check_env_resource.sh`（手动门禁，全资源类型）＋ `scripts/pre-commit-env-gate.sh`（提交级端口冲突阻断，已装 .git/hooks）。规则「先注册后使用」。提交链：885c988→3e58f98→1a43150→439ca66→ed5ff3b→22807df。遗留：他项目（ai-assistant 等）接入需各自授权；`/Users/gogo/ai-assistant-system` 旧副本待其负责人清理。
- **需求评审整改闭环（2026-08-12 整改，2026-08-18 复核通过）**：8项缺陷全部关闭（1轮整改）。新增产出：`requirements/SRS_v0.2/10_安全威胁建模.csv`（SEC-001~008，OWASP TOP10）/ `11_数据生命周期.csv`（7类数据）/ `12_UI交互规范.csv`（8场景）；更新：`04_功能需求.csv`（16条N-REQ补充验收标准）/ `05_性能需求.csv`（REQ-017链接N-REQ）/ `03_外部接口需求.csv`（IF-001~008）/ `08_需求追溯矩阵.csv`（v0.2全16条追溯）。评审结论由⚠️有条件通过升级为✅通过，4项质量门禁全通过，可流转至架构设计阶段。

---

## 2. 项目总目标

通过飞书/Lark 移动端或桌面端调用本机 OpenCode 进行编程开发，交互方式与终端使用 OpenCode 完全一致；按全生命周期（启动→需求→架构→开发→测试→投产→归档）规范化管控。

## 3. 阶段产出物汇总

| 阶段 | 基线 | 产出物 | 状态 |
|------|------|--------|:---:|
| 项目启动 | v0.2 | 台账 17 CSV | ✅ |
| 需求分析 | v0.2 | SRS 16 条 + 评审 5+2 份 + 缺陷整改 8 项 | ✅ |
| 架构设计 | v0.1 | 架构资产 31 份 | ✅ |
| 开发编码 | v0.3 | 代码 + 单测 17 例 | ✅ |
| 测试验收 | v0.4 | 用例 21 组 100% | ✅ |
| 投产部署 | v0.5 | 投产资产 6 目录 | ✅ |
| 归档交接 | v0.5 | 交接文档 + 运维手册 | ✅ |
| 收尾验证 | v2.1 | 真实消息链路验收通过 + P95 补测 + skills 清理 | ✅ |

## 4. 新会话开场话术

```
请先读取「交接文档.md」的「工作断点」区（§1），然后执行：
1. python3 scripts/check_env.py 核验环境（须 test 标记 + 无废弃残留 + OpenClaw 主链路全绿）
2. openclaw channels status 确认 feishu 渠道 running
3. 读 投产资产/06_运维交接/运维手册.md 了解日常运维
4. 如需验证链路，请用户 @机器人 发送测试消息，tail -f /tmp/openclaw/openclaw-*.log 观察 dispatch
```

---

## 5. 版本与知识产权

- **文档版本**：v2.7　**更新日期**：2026-08-18（废弃 webhook 桥彻底移除 + OpenClaw 主链路恢复 + 真实入站验证 + 门禁加固）
- **知识产权所有**：段波（验证邮箱：duanbo.douglas@163.com）
- **技术依据**：IEEE 830 · IEEE 29148:2018 · BABOK v3 · ISO 25010 · OWASP ASVS · PMBOK
