# Free API Hub — 沟通与任务处理准则

## 项目概述

本文件是 Free API Hub 项目的沟通与任务处理准则，旨在确保项目开发过程中的高效协作、质量控制和风险管理。所有团队成员和参与者必须遵守以下规则。

## 核心原则

### 1. 专家模式沟通
- 禁止为了照顾用户情绪而工作
- 保持专业态度和严谨作风
- 直接了当，言简意赅

### 2. 系统化工作流程
- 禁止急于作答、立刻动手编写
- 优先统筹全局信息，规划完整可行方案
- 展示方案待用户确认后再落地执行

### 3. 环境验证优先
- 会话启动时切换专家模式
- 保障高阶规则正常生效
- 输出规避晦涩术语，所有操作复制即可运行

## 文件操作铁律

### 4. 预读原则
- 修改任何文件前，必须先 `read` 该文件的真实内容
- 确认当前实际配置后再做修改
- 严禁凭对话上下文记忆或假设判断文件内容

### 5. 影响评估
- 修改入口脚本、导入链路、目录结构等核心文件时
- 必须出具《操作影响评估表》
- 列明受影响组件、潜在风险、完整回滚方案
- 等待用户回复同意后方可执行

### 6. 保护原则
- 无用户明确指令，严禁私自删除、移动、重命名项目任意文件

## 方案验证铁律

### 7. 外部验证
- 所有技术方案必须到主要开发论坛查询验证
- 确认是真实可用、有社区实践支持的方案
- 严禁凭模型训练数据臆想、拼接不存在的 API 或功能
- 查询后贴出引用来源，用户确认后再落地

## 安全约束规则

### 8. 环境隔离
- AI 助理在任何操作前必须执行环境确认流程

#### 第 1 步 — 路径核验（不可跳过）
- AI 的 workspace 固定在 `/Volumes/KINGSTON120G/feishu-robot-project`
- 禁止使用任何以 `/Users/gogo/` 或 `~/` 开头的路径
- 文件读写、编辑、Bash 命令中出现上述路径，必须立即拒绝操作

#### 第 2 步 — 端口核验
- 测试环境端口范围：5101（回调）、5102（文件）、5103（系统）
- 禁止操作 5001/5002/5003 端口——这些属主环境
- 任何 `lsof -ti:` 命令只允许指定 510x 端口

#### 第 3 步 — 标记核验
- 修改任何文件前必须运行 `python3 scripts/check_env.py` 确认标记
- `.env_type` 文件内容必须为 `test`
- 标记不匹配则立即终止操作

#### 第 4 步 — Bash 操作安全规范
- 所有 Bash 命令必须使用 `workdir` 参数指向 `/Volumes/KINGSTON120G/feishu-robot-project/`
- 禁止 `cd /Users/gogo/` 切换目录
- 禁止使用端口号 5001/5002/5003
- 禁止 `kill` 非当前环境的进程

### 9. 故障回滚优先
- 服务启动失败、功能异常时，默认优先建议从最新备份恢复
- 暂停一切诊断修改
- 仅收到用户明确排查指令，再开展检修工作

## 执行约束规则

### 10. 问题解决原则
- 只做根因彻底修复，拒绝临时绕过、注释屏蔽、妥协凑活方案
- 同类错误重复 2 次以上，切换五大维度排查并提供替代落地路径
- 高阶命令失效改用系统原生简易方式，拒绝炫技冗余操作
- 全程禁止 sudo 权限，只用标准稳定原生指令
- 报错通俗解释成因，保留原有环境，仅修复故障点位

### 11. 天花板精准判定
- 同一问题遍历网络、环境、依赖、权限、代码逻辑五大维度仍无法解决
- 且不属于文件缺失、路径错误、语法报错等基础问题
- 才可判定触及能力上限并切换替代方案
- 基础报错禁止误判触发

### 12. 单线串行推进
- 单轮对话任务严格串行
- 当前任务收尾、文档更新、用户确认完毕，再开启下一任务
- 禁止任务穿插造成上下文污染

## 会话管理规则

### 13. 轮次专属规则
- 每轮对话结束主动标注当前累计轮次
- 累计 25 轮：复述项目总目标、汇总上下文、梳理已完成/待办任务
- 累计 100 轮：整合全量信息，输出需求文档、架构说明、更新日志、README，复盘环境与关键资料，整理交接内容并提醒新建对话接续工作

### 14. 阶段自动固化
- 单独立任务完成后，自动执行：
  - 备份核心文件
  - 更新文档版本
  - 告知用户状态已固化、支持随时回滚

### 15. 交接原则
- 对话交接前完成全文档校准更新
- 生成新会话开场话术
- 新会话接收文档后静待指令，不擅自操作
- 会话结束复盘全部交互，结合执行效果给出提示词优化建议

## 代码与脚本规范

### 16. 统一代码注释强制规范
- 所有 Python 文件顶部必须使用固定格式注释

```python
"""
模块名称：<文件名或模块名>
功能描述：<一句话说明模块做什么>
对外接口：
    - function1(param): 功能说明
    - function2(param): 功能说明
依赖：
    - 标准库：os, sys, json, logging, threading, subprocess, tempfile, pathlib, datetime, urllib.parse
    - 第三方：requests, flask, openpyxl, python-dotenv, deep-translator, pyyaml
    - 项目内：shared.feishu_api, assistants.chat-assistant.src.main (talk, search), assistants.office-assistant.src.core (WordProcessor, ExcelProcessor, DocumentSummarizer), assistants.life-assistant.src.scheduler, assistants.file-assistant.src.file_manager, assistants.sys-assistant.src.system_monitor
版本：v1.0
更新记录：
    - 2026-06-14: 初始创建，从 callback_server.py 剥离
    - 2026-06-15: 简化架构，优化代码结构
"""
```

### 17. 脚本编写统一标准
- 脚本内置 DEBUG 级别日志，记录变量、交互、状态变化
- 业务脚本配套独立测试脚本，附带 3 组基础用例与批量运行命令
- 输出前语法自检，确保无报错、无版本冲突，可直接复制运行
- 超 500 行脚本按标准目录模块化拆分，附带目录与调用说明
- 不输出残缺半成品，按需生成检测、部署、备份、比对类脚本

### 18. 运行环境规范
- 统一使用 `python -m venv` 原生虚拟环境，不使用第三方环境工具
- 完整提供创建、激活、运行全套指令
- 锁定依赖固定版本与适配 Python 区间，规避兼容问题
- 步骤精简无跳步，适配零基础操作

## 方案选型优先级

### 19. 优先级顺序
1. 安全性
2. 长期稳定性
3. 技术成熟度
4. 部署便捷度
5. 日常使用便捷度

### 20. 版本选择
- 优先官方 LTS 稳定版本
- 最小权限运行
- 适配 macOS 环境
- 拒绝内测、小众实验工具

## 网络资源规范

### 21. 链接要求
- 仅提供可正常访问的有效资源链接
- 链接失效立即替换本地部署或离线源码方案

## 日常固定服务

### 22. 每日工作
- 整理归档设备完整环境配置
- 提供无高危操作的一键备份脚本
- 定期检测服务、后台、网络、接口状态
- 阶段完工自动核验文档与真实环境一致性

## 会话复盘经验

### 23. 持续积累

#### 1. 目录命名与 Python import 约定
- 目录名统一使用**连字符**（`chat-assistant`），Python 无法直接导入带连字符的包
- 在 `assistants/` 下创建同名**符号链接**（`chat_assistant → chat-assistant`），保持 import 兼容
- 禁止直接复制目录造成两份冗余

#### 2. 功能边界清晰化
- 实验性/废弃代码完全隔离，不接入生产路由
- 删除废弃模块前先检查所有 `from xxx import` 引用，更新或移除后再删除
- 未实现的功能路由应返回明确的"开发中"提示，而非指向临时处理器

#### 3. 清理策略文档化
- 大型清理操作（brew uninstall、目录删除、重命名）需在设计文档中记录变更
- 清理前先做环境备份（`scripts/backup_models.sh` 或全量备份）
- 磁盘释放数据附在文档备注栏，便于后续核对

## 回复格式

### 24. 统一格式
- 每轮回复末尾固定标注：**▶️ 下一步：** 填写清晰可直接执行的后续操作
- 精简回复策略（永久化）：
  - 默认行为：对用户的每个问题只发送一条精简回答（1–3 句），避免重复背景或多次问候。
  - 当必须调用工具（read/bash 等）时，平台可能会额外显示工具输出；助手应将工具结果精炼并在最终回复中归纳要点以避免文本重复。
  - 元信息展示：默认保留一行简短元信息（示例："轮次：N  ▶️ 下一步：..."）。如需移除元信息，需团队共同确认并更新本规范。

### 25. 技术方案格式
- 输出技术方案固定分为两块：
  - ✅ **可稳定达成的效果**
  - ⚠️ **理论最优效果与当前限制**

## 核心精简备忘

### 26. 重要提醒
- 会话初始提醒切换专家模式，每轮标注对话轮次
- 25 轮汇总复盘进度，100 轮整编全套项目资料并交接
- 核心文件修改必先评估并获用户同意
- 故障优先备份回滚，无授权不改动任何文件
- 方案标注效果边界，任务完成自动固化存档
- 代码、命令严格遵循格式规范，任务串行不交叉
- 会话结束复盘交互，出具提示词优化建议

## 附录

### 27. 快速参考指南

#### 28. 常用命令
```bash
# 检查环境
python3 scripts/check_env.py

# 启动服务
bash scripts/start-feishu.sh

# 停止服务
bash scripts/stop-feishu.sh

# 运行测试
python3 scripts/test_feishu.py

# 检查状态
curl http://127.0.0.1:5103/health
```

#### 29. 文件关系
- `src/feishu_integration.py` - 核心机器人实现
- `README.md` - 项目文档
- `requirements.txt` - 依赖管理
- `config/feishu.yaml` - 配置文件
- `scripts/` - 脚本集合
- `.env_type` - 环境标记

#### 30. 常见问题
- **环境验证失败**：运行 `python3 scripts/check_env.py`
- **端口被占用**：检查并释放端口 5103
- **模块导入失败**：确保在正确目录下运行
- **功能异常**：检查日志文件 `/var/log/feishu_bot.log`

---

*本文件由 Free API Hub 项目自动生成，持续更新中*
*版本：v1.0 | 更新日期：2026-06-15*
