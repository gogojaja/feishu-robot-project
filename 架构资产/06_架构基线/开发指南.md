# 开发指南 — 飞书+OpenClaw+OpenCode（架构基线 v0.3）

> 依据：review.md 环节 4（finalize_baseline）· 开发阶段输入　版本：v0.3　2026-08-12

## 1. 开发输入基线
- 需求基线：SRS v0.3（requirements/SRS_v0.3/，9 章）
- 架构基线：架构资产/01-06（本指南 + 设计说明书 12 章）
- 运行基线：OpenClaw 2026.7.1-2（LaunchAgent 常驻，pid 4639）；opencode 1.17.4

## 2. 技术栈
- OpenClaw 2026.7.1-2（Node26）；@openclaw/feishu；@openclaw/acpx；opencode CLI 1.17.4
- 配置：~/.openclaw/openclaw.json（0600）；环境：test（.env_type=test）
- 端口：18789（OpenClaw 原生）；5101/5102/5103（自研，已停用）

## 3. 配置与待办（M4 输入）

| 配置项 | 状态 | 开发/验证任务 |
|--------|------|---------------|
| acp.enabled=true | 已生效 | ACP smoke test 以 JSON 格式重测（P6） |
| agents.list=[opencode-acp] | 已生效 | 验证 opencode ACP server 实际 spawn |
| bindings=[feishu group] | 已生效 | 群 @ 触发端到端验证 |
| 飞书长连接 | 待切换 | 后台将订阅方式改为长连接（方案 A） |
| dmPolicy=allowlist | 待配置 | 白名单核对 |
| 性能基准 | 待实测 | P95≤15s（REQ-017） |

## 4. 编码/配置规范
- 配置文件含敏感信息须 0600、不入仓库（N-REQ-10）
- 事件日志仅记元数据，不落完整事件体/凭证（N-REQ-11）
- 操作变更前 `python3 scripts/check_env.py` 验证环境（.env_type=test）

## 5. 测试要求
- 配套独立测试脚本（3 组基础用例+批量运行命令，AGENTS.md §16）
- 覆盖：长连接入站/@触发/会话持久化/白名单/权限确认/凭证保护
- 性能基准 v0.3 实测端到端 P95≤15s（REQ-017）

## 6. 验收口径
- 架构评审缺陷全部闭环
- 追溯矩阵断链率 ≤20%；七原则不退化
- 遗留：P6 ACP 重测、飞书订阅切换、性能实测
