# 开发指南 — 飞书机器人项目（架构基线 v0.1）

> 依据：review.md 环节 4（finalize_baseline）· 开发阶段（M4）输入　版本：v0.1　2026-08-05

## 1. 开发输入基线
- 需求基线：SRS v0.1（requirements/SRS_v0.1/）
- 架构基线：架构资产/01-06（本指南 + 设计说明书 12 章）
- 存量代码：src/feishu_integration.py v1.1（Flask 单体）

## 2. 技术栈
- Python 3.9+ / Flask（threaded）/ requests / opencode CLI v1.17.4
- 环境：test（.env_type=test）；端口 5103；配置 env→config/feishu.yaml

## 3. 组件映射与待办（M4 输入）

| 组件 | 状态 | 开发任务 |
|------|------|----------|
| C1-C5 接入/核心 | 存量已有 | 代码走查与测试补充 |
| C6/C7 会话/去重 | 存量已有 | 并发测试验证 |
| C8 OpenCodeRunner | **需改造** | opencode 调用策略 spike：直调→serve+attach 或持久 server（缺陷 ARCH-DEF-001） |
| C9 OutputSanitizer | 需完善 | 截断前代码块保护（REQ-FUNC-REQ-015） |
| C10 RateLimiter | **待开发** | open_id 固定窗口 10 条/分（REQ-SEC-REQ-003） |
| C11 InputValidator | **待开发** | 入站 >4096 字符拒绝（REQ-FUNC-REQ-012） |
| C12 FeishuClient | 需补充 | 发送失败重试 1 次 + token 连续失败≥3 告警（REQ-FUNC-REQ-017/018） |

## 4. 编码规范
- Python 文件顶部固定格式注释（AGENTS.md §15）
- 按需模块化拆分（session_manager / opencode_runner / output_sanitizer / feishu_client），功能边界清晰化
- 未实现路由返回明确「开发中」提示

## 5. 测试要求
- 配套独立测试脚本（3 组基础用例+批量运行命令，AGENTS.md §16）
- 覆盖：challenge/去重/会话连续/限流/入站校验/超时/输出清理
- 性能基准 v0.2 补测端到端 P95≤15s（缺陷 ARCH-DEF-002）

## 6. 验收口径
- 架构评审缺陷全部闭环（ARCH-DEF-001~003）
- 追溯矩阵断链率 ≤20%；七原则不退化
