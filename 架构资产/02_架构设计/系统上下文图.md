# 系统上下文图（C4 Context）— 飞书+OpenClaw+OpenCode（v0.3）

## C4 Context 层

```
┌─────────────────────────────────────────────────────────────┐
│                       用户（项目负责人）                     │
│  飞书/Lark 移动端 · 桌面端 · 群聊@触发 · 私聊白名单          │
└──────────────┬───────────────────────────┬──────────────────┘
               │ @机器人 / 私聊             │ 流式卡片/文本回复
               ▼                           ▲
┌─────────────────────────────────────────────────────────────┐
│                    OpenClaw Gateway（系统）                  │
│  · feishu 渠道（WebSocket 长连接）                          │
│  · 会话路由 bindings / requireMention / dmPolicy            │
│  · Agent 编排 / Skills / 多模型 / MCP                       │
│  · ACP adapter（acpx）→ OpenCode                            │
└──────────────┬───────────────────────────┬──────────────────┘
               │ ACP（stdin/stdout）        │ 状态/日志/指令
               ▼                           ▲
┌─────────────────────────────────────────────────────────────┐
│                 OpenCode（本地 AI 编程 CLI）                 │
│  · ACP server（opencode acp）                              │
│  · 模型：deepseek-v4-flash-free/qwen2.5:7b/glm-4.7-flash   │
│  · 工作目录：/Volumes/KINGSTON120G/feishu-robot-project     │
└─────────────────────────────────────────────────────────────┘

外部依赖：
┌────────────────────┐   ┌────────────────────┐
│  飞书开放平台       │   │  macOS LaunchAgent  │
│  事件长连接+CardKit │   │  常驻/状态检查      │
└────────────────────┘   └────────────────────┘
```

## 边界

- 系统边界：OpenClaw Gateway 内（含 acpx 插件）
- 外部实体：用户（飞书客户端）、OpenCode（ACP 对端）、飞书开放平台、LaunchAgent
- 已废弃：自研 src/feishu_integration.py（webhook 桥接，N-REQ-16 停用）
