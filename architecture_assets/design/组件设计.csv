组件设计

项目：feishu-robot-project — 飞书 → OpenCode 桥接服务
架构师角色：架构师（v21.0.0）
分析日期：2026-08-12
报告版本：v1.0.0

---

## 1. 组件设计概述

### 1.1 组件定义

| 设计项 | 内容 |
|--------|------|
| **组件职责** | 每个组件的单一职责定义 |
| **组件接口** | 对外接口契约（输入/输出/异常） |
| **组件依赖** | 依赖关系图（方向性，避免循环依赖） |
| **组件生命周期** | 创建/初始化/运行/销毁流程 |
| **组件通信** | 同步/异步、协议、数据格式 |

---

## 2. FeishuBot 组件设计

### 2.1 组件职责

| 组件 | 职责 | 单一职责原则 |
|------|------|--------------|
| **消息处理组件** | 解析飞书事件，提取消息内容 | ✅ 单一职责 |
| **限流检查组件** | 检查消息频率，触发限流 | ✅ 单一职责 |
| **OpenCode 调用组件** | 调用 OpenCode 命令行，获取响应 | ✅ 单一职责 |
| **会话管理组件** | 管理会话数据，持久化到文件 | ✅ 单一职责 |
| **消息发送组件** | 发送消息到飞书 | ✅ 单一职责 |
| **Token 获取组件** | 获取飞书 tenant_access_token | ✅ 单一职责 |
| **预热组件** | 启动预热，预加载模型 | ✅ 单一职责 |

### 2.2 组件接口

#### 消息处理组件

| 方法 | 输入 | 输出 | 异常 | 职责 |
|------|------|------|------|------|
| `_process(event)` | event: dict | None | Exception | 解析飞书事件，提取消息内容 |

**接口契约**：
```python
def _process(self, event: dict) -> None:
    """
    处理飞书事件
    
    Args:
        event: 飞书事件数据
        
    Returns:
        None
        
    Raises:
        Exception: 处理异常
    """
```

#### 限流检查组件

| 方法 | 输入 | 输出 | 异常 | 职责 |
|------|------|------|------|------|
| `_check_rate_limit(open_id)` | open_id: str | bool | Exception | 检查消息频率，触发限流 |

**接口契约**：
```python
def _check_rate_limit(self, open_id: str) -> bool:
    """
    固定窗口限流检查
    
    Args:
        open_id: 用户 OpenID
        
    Returns:
        bool: True 表示通过限流，False 表示触发限流
        
    Raises:
        Exception: 限流检查异常
    """
```

#### OpenCode 调用组件

| 方法 | 输入 | 输出 | 异常 | 职责 |
|------|------|------|------|------|
| `_run(message, session_id, timeout)` | message: str, session_id: str, timeout: int | (response: str, new_session_id: str) | Exception | 调用 OpenCode 命令行，获取响应 |

**接口契约**：
```python
def _run(self, message: str, session_id: str = "", timeout: int = 180) -> tuple[str, str]:
    """
    调用 OpenCode 命令行
    
    Args:
        message: 用户消息
        session_id: 会话 ID
        timeout: 超时时间（秒）
        
    Returns:
        tuple[str, str]: (响应文本, 新会话 ID)
        
    Raises:
        Exception: 调用异常
    """
```

#### 会话管理组件

| 方法 | 输入 | 输出 | 异常 | 职责 |
|------|------|------|------|------|
| `_load_sessions()` | None | None | Exception | 从文件加载会话 |
| `_save_sessions()` | None | None | Exception | 保存会话到文件 |

**接口契约**：
```python
def _load_sessions(self) -> None:
    """
    从 var/sessions.json 加载会话
    
    Returns:
        None
        
    Raises:
        Exception: 加载异常
    """
```

```python
def _save_sessions(self) -> None:
    """
    保存会话到 var/sessions.json
    
    Returns:
        None
        
    Raises:
        Exception: 保存异常
    """
```

#### 消息发送组件

| 方法 | 输入 | 输出 | 异常 | 职责 |
|------|------|------|------|------|
| `send(receive_id, text, receive_id_type, mention_id)` | receive_id: str, text: str, receive_id_type: str, mention_id: str | bool | Exception | 发送消息到飞书 |

**接口契约**：
```python
def send(self, receive_id: str, text: str, receive_id_type: str = "open_id", mention_id: str = "") -> bool:
    """
    发送消息到飞书
    
    Args:
        receive_id: 接收者 ID（open_id 或 chat_id）
        text: 消息内容
        receive_id_type: 接收者类型（open_id 或 chat_id）
        mention_id: 群聊 @ 用户 ID
        
    Returns:
        bool: True 表示发送成功，False 表示发送失败
        
    Raises:
        Exception: 发送异常
    """
```

#### Token 获取组件

| 方法 | 输入 | 输出 | 异常 | 职责 |
|------|------|------|------|------|
| `get_token()` | None | bool | Exception | 获取飞书 tenant_access_token |

**接口契约**：
```python
def get_token(self) -> bool:
    """
    获取飞书 tenant_access_token
    
    Returns:
        bool: True 表示获取成功，False 表示获取失败
        
    Raises:
        Exception: 获取异常
    """
```

#### 预热组件

| 方法 | 输入 | 输出 | 异常 | 职责 |
|------|------|------|------|------|
| `_warmup()` | None | None | Exception | 启动预热，预加载模型 |

**接口契约**：
```python
def _warmup(self) -> None:
    """
    启动预热，预加载模型
    
    Returns:
        None
        
    Raises:
        Exception: 预热异常
    """
```

---

## 3. 组件依赖关系

### 3.1 依赖关系图

```
FeishuBot 类
├── 消息处理组件
│   ├── 限流检查组件
│   └── OpenCode 调用组件
│       └── 消息发送组件
│           └── Token 获取组件
├── 会话管理组件
│   └── 会话管理组件
├── 预热组件
│   └── OpenCode 调用组件
└── API 路由组件
    ├── 消息处理组件
    ├── 会话管理组件
    └── Token 获取组件
```

### 3.2 依赖关系说明

| 依赖方向 | 依赖组件 | 说明 |
|----------|----------|------|
| 消息处理组件 → 限流检查组件 | 限流检查组件 | 检查消息频率 |
| 消息处理组件 → OpenCode 调用组件 | OpenCode 调用组件 | 调用 OpenCode |
| OpenCode 调用组件 → 消息发送组件 | 消息发送组件 | 发送响应 |
| 消息发送组件 → Token 获取组件 | Token 获取组件 | 获取 Token |
| API 路由组件 → 消息处理组件 | 消息处理组件 | 处理消息 |
| API 路由组件 → 会话管理组件 | 会话管理组件 | 管理会话 |
| API 路由组件 → Token 获取组件 | Token 获取组件 | 获取 Token |
| 预热组件 → OpenCode 调用组件 | OpenCode 调用组件 | 预热调用 |

### 3.3 循环依赖检查

✅ **无循环依赖**

---

## 4. 组件生命周期

### 4.1 FeishuBot 生命周期

| 阶段 | 方法 | 说明 |
|------|------|------|
| **初始化** | `__init__()` | 初始化 FeishuBot，加载会话 |
| **启动** | `start(port)` | 启动 Flask 服务，注册路由 |
| **预热** | `_warmup()` | 启动预热，预加载模型 |
| **运行** | `_process()` | 处理飞书事件 |
| **停止** | `stop()` | 停止 Flask 服务（由 Flask 内部处理） |

### 4.2 组件生命周期状态

```
┌─────────────────────────────────────────────────────────────┐
│                    FeishuBot 生命周期                        │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  初始化 (__init__)                                          │
│  - 加载会话数据                                              │
│  - 初始化 Token                                              │
│  - 初始化限流记录                                            │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  启动 (start)                                               │
│  - 注册路由                                                  │
│  - 启动 Flask 服务                                           │
│  - 启动预热线程                                              │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  运行 (_process)                                            │
│  - 接收飞书事件                                              │
│  - 处理消息                                                  │
│  - 调用 OpenCode                                             │
│  - 发送响应                                                  │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  停止 (stop)                                                │
│  - 停止 Flask 服务                                           │
│  - 保存会话数据                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 5. 组件通信

### 5.1 同步通信

| 组件 | 通信方式 | 协议 | 数据格式 |
|------|----------|------|----------|
| 消息处理组件 → 限流检查组件 | 方法调用 | 同步 | open_id: str |
| 消息处理组件 → OpenCode 调用组件 | 方法调用 | 同步 | message: str, session_id: str |
| OpenCode 调用组件 → 消息发送组件 | 方法调用 | 同步 | response: str |
| API 路由组件 → 消息处理组件 | 方法调用 | 同步 | event: dict |

### 5.2 异步通信

| 组件 | 通信方式 | 协议 | 数据格式 |
|------|----------|------|----------|
| 主线程 → 工作线程 | 线程启动 | 异步 | event: dict |
| 工作线程 → 主线程 | 返回值 | 同步 | response: str, new_session_id: str |

### 5.3 通信协议

| 通信类型 | 协议 | 说明 |
|----------|------|------|
| **组件间通信** | 方法调用 | 同步通信，直接调用 |
| **线程间通信** | 线程启动 + 返回值 | 异步通信，通过返回值传递数据 |
| **HTTP 通信** | HTTP/HTTPS | 飞书 API、OpenCode API |

---

## 6. 组件设计总结

### 6.1 组件职责检查

| 组件 | 职责单一性 | 依赖清晰度 | 生命周期完整 | 通信明确 |
|------|------------|------------|--------------|----------|
| 消息处理组件 | ✅ | ✅ | ✅ | ✅ |
| 限流检查组件 | ✅ | ✅ | ✅ | ✅ |
| OpenCode 调用组件 | ✅ | ✅ | ✅ | ✅ |
| 会话管理组件 | ✅ | ✅ | ✅ | ✅ |
| 消息发送组件 | ✅ | ✅ | ✅ | ✅ |
| Token 获取组件 | ✅ | ✅ | ✅ | ✅ |
| 预热组件 | ✅ | ✅ | ✅ | ✅ |

### 6.2 设计原则检查

✅ **所有组件均满足单一职责原则**
✅ **无循环依赖**
✅ **生命周期完整**
✅ **通信明确**

---

**报告版本**：v1.0.0
**最后更新**：2026-08-12
**知识产权所有**：段波（验证邮箱：duanbo.douglas@163.com）
