接口设计

项目：feishu-robot-project — 飞书 → OpenCode 桥接服务
架构师角色：架构师（v21.0.0）
分析日期：2026-08-12
报告版本：v1.0.0

---

## 1. 接口设计概述

### 1.1 接口设计项

| 设计项 | 内容 |
|--------|------|
| **API 契约** | REST/RPC/gRPC 接口定义 |
| **版本管理** | API 版本策略（URL/Header/SemVer） |
| **错误码规范** | 统一错误码体系、错误响应格式 |
| **认证授权** | 接口认证机制（OAuth2/JWT/API Key） |
| **限流熔断** | 接口级限流策略、熔断降级规则 |

---

## 2. REST API 接口设计

### 2.1 Webhook 接口

#### 2.1.1 飞书事件 Webhook

**接口路径**：`/feishu/events`

**请求方法**：POST

**请求头**：
```
Content-Type: application/json
X-Feishu-Signature: <签名> (可选)
```

**请求体**：
```json
{
  "type": "event_callback",
  "header": {
    "event_type": "im.message.receive_v1",
    "app_id": "cli_xxx",
    "event_id": "ev_xxx",
    "token": "xxx"
  },
  "event": {
    "type": "im.message.receive_v1",
    "message": {
      "message_type": "text",
      "content": "{\"text\": \"Hello\"}",
      "message_id": "msg_xxx",
      "chat_id": "oc_xxx",
      "chat_type": "group"
    },
    "sender": {
      "sender_id": {
        "open_id": "ou_xxx"
      }
    }
  }
}
```

**响应**：
```json
{
  "status": "ok"
}
```

**错误响应**：
```json
{
  "status": "duplicate"
}
```

**错误码**：
- `200` - 成功
- `400` - 请求参数错误
- `401` - 认证失败
- `403` - 权限不足
- `500` - 服务器内部错误

**认证授权**：
- 无需认证（飞书平台验证签名）

**限流**：
- 固定窗口限流：10 条/分钟

---

#### 2.1.2 系统事件 Webhook

**接口路径**：`/webhook_sys`

**请求方法**：POST

**请求头**：
```
Content-Type: application/json
```

**请求体**：同 `/feishu/events`

**响应**：
```json
{
  "status": "ok"
}
```

**错误响应**：
```json
{
  "status": "skip"
}
```

---

#### 2.1.3 群聊事件 Webhook

**接口路径**：`/webhook_chat`

**请求方法**：POST

**请求头**：
```
Content-Type: application/json
```

**请求体**：同 `/feishu/events`

**响应**：同 `/feishu/events`

---

### 2.2 REST API 接口

#### 2.2.1 聊天接口

**接口路径**：`/api/chat`

**请求方法**：POST

**请求头**：
```
Content-Type: application/json
```

**请求体**：
```json
{
  "message": "Hello",
  "session_id": "session_xxx"
}
```

**响应**：
```json
{
  "response": "Hello! How can I help you?",
  "session_id": "session_xxx"
}
```

**错误响应**：
```json
{
  "error": "empty message"
}
```

**错误码**：
- `200` - 成功
- `400` - 请求参数错误
- `500` - 服务器内部错误

**认证授权**：
- 无需认证（可选：API Key）

**限流**：
- 固定窗口限流：10 条/分钟

---

### 2.3 Web 页面接口

#### 2.3.1 聊天页面

**接口路径**：`/chat`

**请求方法**：GET

**响应**：HTML 页面

**功能**：
- 用户输入消息
- 显示历史消息
- 自动刷新会话 ID

---

### 2.4 健康检查接口

#### 2.4.1 健康检查

**接口路径**：`/health`

**请求方法**：GET

**响应**：
```json
{
  "status": "ok"
}
```

**错误响应**：
```json
{
  "status": "error"
}
```

**认证授权**：
- 无需认证

---

## 3. API 版本管理

### 3.1 版本策略

| 策略 | 说明 |
|------|------|
| **URL 版本** | 不使用 URL 版本（v1） |
| **Header 版本** | 不使用 Header 版本 |
| **SemVer** | 使用 PATCH 版本 |

### 3.2 版本兼容性

| 版本 | 说明 | 兼容性 |
|------|------|--------|
| **v1.0** | 初始版本 | - |

---

## 4. 错误码规范

### 4.1 错误码体系

| 错误码 | 说明 | HTTP 状态码 |
|--------|------|-------------|
| `200` | 成功 | 200 |
| `400` | 请求参数错误 | 400 |
| `401` | 认证失败 | 401 |
| `403` | 权限不足 | 403 |
| `500` | 服务器内部错误 | 500 |
| `503` | 服务不可用 | 503 |

### 4.2 错误响应格式

```json
{
  "error": "error_message",
  "code": "error_code",
  "details": {}
}
```

---

## 5. 认证授权

### 5.1 认证机制

| 接口 | 认证方式 | 说明 |
|------|----------|------|
| `/feishu/events` | 飞书平台验证签名 | 飞书平台验证 |
| `/api/chat` | 无需认证（可选：API Key） | 可选认证 |
| `/health` | 无需认证 | 健康检查 |

### 5.2 授权机制

| 接口 | 授权方式 | 说明 |
|------|----------|------|
| `/feishu/events` | 无需授权 | 飞书平台验证 |
| `/api/chat` | 无需授权 | 可选授权 |
| `/health` | 无需授权 | 健康检查 |

---

## 6. 限流熔断

### 6.1 限流策略

| 接口 | 限流方式 | 限流规则 | 说明 |
|------|----------|----------|------|
| `/feishu/events` | 固定窗口 | 10 条/分钟 | 飞书事件限流 |
| `/api/chat` | 固定窗口 | 10 条/分钟 | REST API 限流 |
| `/health` | 无限流 | - | 健康检查不限流 |

### 6.2 熔断降级

| 接口 | 熔断策略 | 降级策略 |
|------|----------|----------|
| `/feishu/events` | Token 失败熔断 | 仅 health 可用 |
| `/api/chat` | OpenCode 超时熔断 | 返回错误提示 |
| `/health` | 无熔断 | - |

---

## 7. 接口设计总结

### 7.1 接口清单

| 接口路径 | 请求方法 | 认证 | 限流 | 说明 |
|----------|----------|------|------|------|
| `/feishu/events` | POST | 飞书签名 | 10 条/分钟 | 飞书事件 Webhook |
| `/webhook_sys` | POST | 无 | 10 条/分钟 | 系统事件 Webhook |
| `/webhook_chat` | POST | 无 | 10 条/分钟 | 群聊事件 Webhook |
| `/api/chat` | POST | 无 | 10 条/分钟 | REST API 聊天 |
| `/chat` | GET | 无 | 无 | Web 聊天页面 |
| `/health` | GET | 无 | 无 | 健康检查 |

### 7.2 接口设计原则

✅ **RESTful 风格**
✅ **统一错误码**
✅ **认证授权明确**
✅ **限流熔断完善**

---

**报告版本**：v1.0.0
**最后更新**：2026-08-12
**知识产权所有**：段波（验证邮箱：duanbo.douglas@163.com）
