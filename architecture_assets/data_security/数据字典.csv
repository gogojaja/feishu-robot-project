数据字典

项目：feishu-robot-project — 飞书 → OpenCode 桥接服务
架构师角色：架构师（v21.0.0）
分析日期：2026-08-12
报告版本：v1.0.0

---

## 1. 数据字典概述

### 1.1 数据字典定义

| 设计项 | 内容 |
|--------|------|
| **字段定义** | 字段名、数据类型、长度 |
| **约束** | 非空、唯一、默认值、外键约束 |
| **值域** | 枚举/范围、格式校验规则 |

---

## 2. Session 表数据字典

### 2.1 字段定义

| 字段名 | 数据类型 | 长度 | 说明 |
|--------|----------|------|------|
| **session_id** | string | - | 会话 ID |
| **user_open_id** | string | - | 用户 OpenID |
| **created_at** | timestamp | - | 创建时间 |
| **updated_at** | timestamp | - | 更新时间 |

### 2.2 约束

| 约束类型 | 约束内容 | 说明 |
|----------|----------|------|
| **主键** | session_id | 会话 ID 为主键 |
| **外键** | user_open_id → User.open_id | 用户 OpenID 外键 |
| **非空** | session_id, user_open_id, created_at, updated_at | 必填字段 |

### 2.3 值域

| 字段名 | 值域 | 格式校验 |
|--------|------|----------|
| **session_id** | UUID | UUID 格式 |
| **user_open_id** | string | 飞书 OpenID 格式 |
| **created_at** | timestamp | ISO 8601 格式 |
| **updated_at** | timestamp | ISO 8601 格式 |

---

## 3. Message 表数据字典

### 3.1 字段定义

| 字段名 | 数据类型 | 长度 | 说明 |
|--------|----------|------|------|
| **message_id** | string | - | 消息 ID |
| **session_id** | string | - | 会话 ID |
| **content** | string | - | 消息内容 |
| **timestamp** | timestamp | - | 时间戳 |
| **sender** | string | - | 发送者 ID |

### 3.2 约束

| 约束类型 | 约束内容 | 说明 |
|----------|----------|------|
| **主键** | message_id | 消息 ID 为主键 |
| **外键** | session_id → Session.session_id | 会话 ID 外键 |
| **非空** | message_id, session_id, content, timestamp, sender | 必填字段 |

### 3.3 值域

| 字段名 | 值域 | 格式校验 |
|--------|------|----------|
| **message_id** | string | 飞书消息 ID 格式 |
| **session_id** | UUID | UUID 格式 |
| **content** | string | UTF-8 字符 |
| **timestamp** | timestamp | ISO 8601 格式 |
| **sender** | string | 飞书用户 ID 格式 |

---

## 4. FeishuBot 内存数据字典

### 4.1 字段定义

| 字段名 | 数据类型 | 长度 | 说明 |
|--------|----------|------|------|
| **tenant_access_token** | string | - | 飞书 Token |
| **token_expire_time** | int | - | Token 过期时间（Unix 时间戳） |
| **_token_fail_count** | int | - | Token 失败计数 |
| **sessions** | dict | - | 会话字典 |
| **_processed_ids** | set | - | 消息 ID 去重集合 |
| **_rate_limits** | dict | - | 限流记录 |

### 4.2 约束

| 约束类型 | 约束内容 | 说明 |
|----------|----------|------|
| **唯一性** | tenant_access_token | Token 唯一 |
| **非空** | tenant_access_token, token_expire_time, _token_fail_count | 必填字段 |

### 4.3 值域

| 字段名 | 值域 | 格式校验 |
|--------|------|----------|
| **tenant_access_token** | string | JWT 格式 |
| **token_expire_time** | int | Unix 时间戳（秒） |
| **_token_fail_count** | int | 0-3（连续失败次数） |
| **sessions** | dict | 字典格式 |
| **_processed_ids** | set | 集合格式 |
| **_rate_limits** | dict | 字典格式 |

---

## 5. 数据字典总结

### 5.1 数据表汇总

| 表名 | 字段数 | 主键 | 外键 | 索引 |
|------|--------|------|------|------|
| **Session** | 4 | session_id | user_open_id | user_open_id |
| **Message** | 5 | message_id | session_id | session_id |
| **FeishuBot 内存** | 6 | - | - | - |

### 5.2 数据类型汇总

| 数据类型 | 使用场景 |
|----------|----------|
| **string** | 会话 ID、用户 ID、消息 ID、消息内容 |
| **timestamp** | 创建时间、更新时间、时间戳 |
| **int** | Token 过期时间、Token 失败计数 |
| **dict** | 会话字典、限流记录 |
| **set** | 消息 ID 去重集合 |

### 5.3 数据完整性

| 数据完整性 | 说明 |
|------------|------|
| **实体完整性** | 主键唯一性 | ✅ |
| **参照完整性** | 外键约束 | ✅ |
| **域完整性** | 字段类型、长度、非空约束 | ✅ |
| **用户定义完整性** | 字段约束、值域 | ✅ |

---

**报告版本**：v1.0.0
**最后更新**：2026-08-12
**知识产权所有**：段波（验证邮箱：duanbo.douglas@163.com）
