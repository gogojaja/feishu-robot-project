# 架构事实基线（单一事实源）

> 项目：feishu-robot-project 飞书机器人（feishu-robot-project）
> 用途：记录**实际生效**的架构链路与环境事实，作为会话排查的权威依据（优先级 > 交接文档叙述）
> 建立：2026-08-18　维护：每次环境/配置变更后同步刷新

---

## 1. 当前生效架构（权威）

**主链路（既定架构，ADR-001 已接受）**：

```
飞书客户端 → 飞书开放平台（长连接 WebSocket 事件）
          → OpenClaw Gateway（127.0.0.1:18789，feishu channel 长连接）
          → acpx 插件 → opencode ACP Runtime（persistent）
          → 回复同链路返回飞书
```

- 事件接入：`im.message.receive_v1` 经 **WebSocket 长连接** 推送（免公网/免 ngrok）
- OpenClaw 配置：`~/.openclaw/openclaw.json`（版本 2026.7.1-2，LaunchAgent 常驻）
- 飞书应用：`cli_aa82c1b457b89bc3`（机器人名 "OpenClaw+Opencode"）
- 绑定：feishu channel default → agent `opencode-acp`（acp persistent，cwd=feishu-robot-project）
- 白名单：DM 仅 `ou_beb9dba11a9eebe2a446f06a028ee092`；群 `oc_7e3442d95ddf0b3c226cb528a4db2ced`；requireMention=true
- LLM：ollama（11434）qwen2.5:7b / gemma4

**废弃过渡链路（已彻底移除，2026-08-18）**：

- 自研 Flask Webhook（5103）+ opencode serve（5102）+ ngrok（employee-radish-fringe）已全部停止并移出主树，归档 `备份/废弃_webhook桥_20260818/`
- `config/` 已清空；`check_env.py` 新增废弃残留阻断（废弃代码/5102/5103/ngrok 任一命中即红灯），本基线不再保留该链路配置详情

## 2. 环境事实（本次校准记录）

| 项 | 状态（2026-08-18） | 说明 |
|---|---|---|
| OpenClaw 安装 | ✅ `/opt/homebrew/bin/openclaw` v2026.7.1-2 | npm 全局 |
| Gateway 18789 | ✅ 运行中 | LaunchAgent `ai.openclaw.gateway` |
| feishu channel | ✅ running/connected | 真实入站验证通过（23:26/23:27） |
| ollama 11434 | 归属 ai-assistant-system | 共用 inference |
| Flask 5103 / serve 5102 / ngrok | ✅ 已全部停止释放 | 废弃 webhook 桥移除后无监听 |

## 3. 关键配置历史与根因教训

- **2026-08-11/12**：`openclaw.json` 将 feishu appSecret 改为 `env:FEISHU_APP_SECRET` 引用，但 **env secret provider 从未配置** → Gateway 每次启动失败 edge（`SecretProviderResolutionError`）→ crash-loop breaker 启动抑制 → OpenClaw 链路自 8-12 起**瘫痪**
- **2026-08-18**：恢复 appSecret 明文（与 `~/.openclaw/openclaw.json` 内 `channels.feishu.accounts.default` 一致）→ Gateway 正常启动；备份 `openclaw.json.bak-20260818*`
- **2026-08-18（废弃闭环）**：webhook 桥（5103 Flask + 5102 serve + ngrok + launchd plist + config/feishu*.yaml）已全部停止、移出主树、归档 `备份/废弃_webhook桥_20260818/`；`check_env.py`/`pre-commit-env-gate.sh` 新增废弃残留阻断
- 教训：**配置变更必须带验证门禁**（见 scripts/openclaw-gate-check.sh），禁止改完不验证；**功能废弃须物理移除+文档收敛+门禁防复活**（Meta SCARF 实践）

## 4. 配置变更门禁流程（强制）

修改 `~/.openclaw/openclaw.json` 或飞书平台配置后必须执行：

```bash
bash scripts/openclaw-gate-check.sh   # 备份/校验/重启/验证四步
bash scripts/check_env.py            # 返回所有检查通过
```

验证通过标准：`openclaw channels status` 中 Feishu 为 running，`openclaw logs` 出现 `feishu ws connected` / provider ready。

### 4.1 ~~遗留待办：流式卡片 HTTP 400~~（2026-08-19 已闭环 ✅）

- **现象**：每次回复先创建流式卡片失败（`POST cardkit/v1/cards` → HTTP 400 code=99991672），自动回退非流式卡片
- **根因（2026-08-19 精确定位）**：`cardkit:card:write` 仅授权为 **user 身份**（scope_type=user），未授权 **tenant 身份**；OpenClaw 用 tenant_access_token 调用 → 报「应用尚未开通所需的应用身份权限」
- **处置（2026-08-19 已落地）**：飞书开放平台 → 权限管理 → 批量导入，将 `cardkit:card:write` 加入 `tenant` 数组 → 申请开通 → 创建新版本发布
- **验证（2026-08-19 22:22）**：① 诊断脚本 cardkit 调用返回 200 + card_id；② 重启 Gateway 后真实消息触发 `Started streaming` + `Closed streaming` 全程成功，0 ERROR；详见 `投产资产/06_运维交接/排查交接_流式卡片400_20260819.md`

## 5. 启动/停止/巡检常用命令

```bash
openclaw gateway status / restart / stop   # Gateway 守护（LaunchAgent）
openclaw channels status                   # 渠道健康
openclaw logs --follow                     # 实时日志
openclaw pairing list feishu               # 待配对（一次性）
bash scripts/openclaw-gate-check.sh        # 配置变更门禁+全量验证
```