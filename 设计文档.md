# Free API Hub — 飞书/Lark 机器人集成项目设计文档

## 项目概述

本项目旨在实现基于飞书/Lark 平台的移动端编程交互系统，允许用户通过飞书/Lark 与 AI 编程助手进行对话和编程操作。项目基于现有的 Free API Hub 架构，但进行了全面简化，以实现更轻量级、更易于部署的解决方案。

## 设计目标

### 1. 架构简化
- 将复杂的多组件架构整合为单一 Python 文件
- 消除不必要的依赖和组件
- 实现易于部署、易于维护的解决方案

### 2. 移动端友好
- 支持飞书/Lark 移动端客户端
- 保持与 open code 相似的交互方式
- 提供流畅的编程体验

### 3. 功能完整
- 消息交互
- 代码执行
- 文件操作
- 命令执行
- 安全验证

## 技术架构

### 1. 核心组件

#### 飞书/Lark 机器人 (`src/feishu_integration.py`)
- **职责**：消息处理、代码执行、文件操作、命令执行
- **特点**：单一文件实现，所有功能集成
- **优势**：部署简单，维护方便

#### 支持脚本
- `scripts/start-feishu.sh`：启动飞书机器人
- `scripts/stop-feishu.sh`：停止飞书机器人
- `scripts/test_feishu.py`：测试脚本
- `scripts/check_env.py`：环境验证脚本

### 2. 数据流

```
飞书/Lark 用户 → 飞书机器人 → AI 编程助手 → 模型响应 → 飞书/Lark 用户
```

### 3. 模块设计

#### FeishuBot 类
- **属性**：app_id、app_secret、verification_token、tenant_access_token
- **方法**：
  - `__init__`：初始化机器人
  - `setup_logging`：配置日志
  - `get_tenant_access_token`：获取访问令牌
  - `send_message`：发送消息
  - `handle_message`：处理消息
  - `process_text_message`：处理文本消息
  - `is_code_request`：检查是否为代码请求
  - `process_code_request`：处理代码请求
  - `format_execution_result`：格式化执行结果
  - `start_server`：启动消息服务器

#### 辅助函数
- `setup_feishu_app`：创建飞书应用并返回配置
- `main`：主函数

## 设计决策

### 1. 架构选择
**选择单一文件架构**，基于以下原因：

- **成熟方案参考**：参考 GitHub 上许多成功的单体架构项目
- **部署简单**：无需复杂的组件管理
- **维护方便**：所有代码在一个文件中
- **性能优化**：减少组件间通信开销

### 2. 技术选型

#### Python
- **理由**：成熟的编程语言，丰富的库支持，易于学习
- **版本**：3.9 或更高版本

#### Flask
- **理由**：轻量级 Web 框架，易于集成
- **特点**：支持异步处理，性能优异

#### requests
- **理由**：最流行的 HTTP 客户端库
- **特点**：功能强大，易于使用

### 3. 代码结构

#### 模块化设计
- **单一类设计**：FeishuBot 类封装所有功能
- **方法职责明确**：每个方法有单一职责
- **良好的封装性**：内部实现细节隐藏

#### 错误处理
- **异常捕获**：所有异常都捕获并处理
- **日志记录**：详细的日志记录
- **用户友好**：友好的错误消息

#### 安全性
- **消息验证**：支持消息签名验证
- **访问控制**：支持用户白名单
- **令牌管理**：自动令牌管理

## 实现计划

### 阶段 1：环境准备
1. 创建项目目录结构
2. 编写环境验证脚本
3. 配置开发环境
4. 安装依赖

### 阶段 2：核心功能实现
1. 编写 FeishuBot 类
2. 实现消息处理
3. 实现代码执行
4. 实现文件操作
5. 实现命令执行

### 阶段 3：辅助功能实现
1. 编写辅助函数
2. 编写测试脚本
3. 编写启动/停止脚本
4. 编写环境验证脚本

### 阶段 4：文档编写
1. 编写项目文档
2. 编写代码注释
3. 编写使用说明
4. 编写开发指南

### 阶段 5：测试与验证
1. 运行测试套件
2. 验证功能正确性
3. 验证性能指标
4. 验证安全性

## 代码规范

### 1. 注释规范
```python
"""
模块名称：<文件名或模块名>
功能描述：<一句话说明模块做什么>
对外接口：
    - function1(param): 功能说明
    - function2(param): 功能说明
依赖：
    - 标准库：os, sys, json, logging, threading, subprocess, tempfile, pathlib, datetime, urllib.parse
    - 第三方：requests, flask, openpyxl, python-dotenv, deep-translator, pyyaml
    - 项目内：shared.feishu_api, assistants.chat-assistant.src.main (talk, search), assistants.office-assistant.src.core (WordProcessor, ExcelProcessor, DocumentSummarizer), assistants.life-assistant.src.scheduler, assistants.file-assistant.src.file_manager, assistants.sys-assistant.src.system_monitor
版本：v1.0
更新记录：
    - 2026-06-14: 初始创建，从 callback_server.py 剥离
    - 2026-06-15: 简化架构，优化代码结构
"""
```

### 2. 命名规范
- **类名**：PascalCase，如 `FeishuBot`
- **方法名**：camelCase，如 `get_tenant_access_token`
- **变量名**：snake_case，如 `tenant_access_token`
- **常量名**：UPPER_SNAKE_CASE，如 `APP_ID`

### 3. 代码风格
- **缩进**：4 个空格
- **行长**：最大 79 字符
- **导入顺序**：标准库、第三方库、项目内库
- **文档字符串**：使用 triple quotes

## 测试策略

### 1. 测试脚本
`scripts/test_feishu.py` 包含以下测试：

- **集成测试**：测试飞书集成功能
- **消息处理测试**：测试消息处理
- **代码执行测试**：测试代码执行
- **集成测试**：测试与 Free API Hub 的集成

### 2. 测试用例
每个测试包含 3 组基础用例：

1. **正常情况测试**：测试正常情况
2. **异常情况测试**：测试异常情况
3. **边界情况测试**：测试边界情况

### 3. 测试工具
- **pytest**：测试框架
- **unittest**：单元测试
- **mock**：模拟对象

## 部署指南

### 1. 环境准备
```bash
# 检查环境
python3 scripts/check_env.py

# 安装依赖
pip install -r requirements.txt
```

### 2. 启动服务
```bash
# 启动飞书机器人
bash scripts/start-feishu.sh
```

### 3. 检查状态
```bash
# 检查状态
curl http://127.0.0.1:5103/health
```

### 4. 停止服务
```bash
# 停止飞书机器人
bash scripts/stop-feishu.sh
```

## 风险管理

### 1. 技术风险
- **风险**：代码复杂度高
- **缓解措施**：采用单一文件架构
- **影响**：低

- **风险**：依赖第三方库
- **缓解措施**：使用流行的稳定库
- **影响**：低

### 2. 安全风险
- **风险**：消息安全
- **缓解措施**：支持消息签名验证
- **影响**：中

- **风险**：令牌泄露
- **缓解措施**：自动令牌管理
- **影响**：中

### 3. 性能风险
- **风险**：性能瓶颈
- **缓解措施**：优化代码，实现异步处理
- **影响**：低

## 项目状态

### 当前状态
- ✅ 架构设计完成
- ✅ 核心代码编写完成
- ✅ 辅助脚本编写完成
- ✅ 文档编写完成
- ✅ 测试脚本编写完成
- ✅ 环境验证完成

### 下一步
- 运行测试套件
- 验证功能正确性
- 优化代码性能
- 完善文档

## 结论

本项目实现了基于飞书/Lark 平台的移动端编程交互系统，具有以下特点：

- **架构简化**：单一文件实现
- **功能完整**：消息处理、代码执行、文件操作、命令执行
- **安全可靠**：消息验证、访问控制、令牌管理
- **易于部署**：简单的部署流程
- **易于维护**：单一文件维护

项目已准备就绪，可以开始执行了！ 🚀