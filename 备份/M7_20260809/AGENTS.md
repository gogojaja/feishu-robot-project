# Free API Hub — 沟通与任务处理准则

> 全局准则：与 DevProjectTeamSkill（.opencode/skills/）重复的规则由技能包承载，本文件仅保留项目特有约束与补充细则。
> 编号说明：前 11 条保持原编号（铁律引用稳定），后续章节因删除重复项顺延。

## 核心原则

### 1. 专家模式沟通
- 禁止情绪化工作；保持专业严谨、直接了当、言简意赅

### 2. 系统化工作流程
- 禁止急于作答；先统筹全局规划完整方案，展示待确认后再落地执行

### 3. 环境验证优先
- 会话启动切专家模式；输出规避晦涩术语，操作复制即可运行

## 文件操作铁律

### 4. 预读原则
- 修改任何文件前必须先 `read` 真实内容，严禁凭对话记忆或假设判断

### 5. 影响评估
- 修改核心文件（入口脚本/导入链路/目录结构）必须出《操作影响评估表》：受影响组件、潜在风险、完整回滚方案，获用户同意后方可执行

### 6. 保护原则
- 无用户明确指令，严禁删除、移动、重命名项目任何文件

## 方案验证铁律

### 7. 外部验证
- 技术方案须到主要开发论坛查询验证真实可用，严禁臆想/拼接 API；贴出引用来源，用户确认后再落地

## 安全约束规则

### 8. 环境隔离（任何操作前执行）
1. **路径**：workspace 固定 `/Volumes/KINGSTON120G/feishu-robot-project`；禁 `/Users/gogo/` 与 `~/` 路径
2. **端口**：仅 5101/5102/5103；禁 5001/5002/5003；`lsof -ti:` 仅限 510x
3. **标记**：修改文件前运行 `python3 scripts/check_env.py`，`.env_type` 须为 `test`，不匹配立即终止
4. **Bash**：用 `workdir` 指向项目根；禁 `cd /Users/gogo/`；禁 kill 非本环境进程

### 9. 故障回滚优先
- 服务异常默认从最新备份恢复，暂停诊断；仅收用户明确排查指令再检修

## 执行约束规则

### 10. 问题解决原则
- 只做根因彻底修复，拒绝绕过/注释屏蔽；同类错误≥2次换五维排查；全程禁 sudo；报错通俗解释成因，保留原环境仅修复故障点

### 11. 天花板精准判定
- 五维（网络/环境/依赖/权限/代码）遍历仍无解，且非文件缺失/路径错误/语法报错等基础问题，才判定触及上限切换方案

## 会话管理规则

### 12. 轮次专属规则
- 每轮对话末尾标注累计轮次；25 轮复述项目总目标、汇总上下文、梳理已完成/待办；100 轮整编需求文档/架构说明/更新日志/README 并交接

### 13. 阶段自动固化
- 独立任务完成即自动备份核心文件、更新文档版本、告知可回滚

### 14. 交接原则
- 交接前校准全文档并生成开场话术；新会话静待指令；会话结束复盘交互并出提示词优化建议

## 代码与脚本规范

### 15. 统一代码注释强制规范
- 所有 Python 文件顶部必须使用固定格式注释：

```python
"""
模块名称：<文件名或模块名>
功能描述：<一句话说明模块做什么>
对外接口：
    - function(param): 功能说明
依赖：
    - 标准库：os, sys, json, logging, threading, subprocess, pathlib, time
    - 第三方：requests, flask, pyyaml
    - 项目内：（按实际依赖填写）
版本：v1.0
更新记录：
    - YYYY-MM-DD: 说明
"""
```

### 16. 脚本编写统一标准
- 脚本内置 DEBUG 级日志；业务脚本配套独立测试脚本（3 组基础用例+批量运行命令）；输出前语法自检；超 500 行按目录模块化拆分；不输出残缺半成品

### 17. 运行环境规范
- 统一 `python -m venv` 原生虚拟环境；锁定依赖固定版本与适配 Python 区间；步骤精简无跳步

## 方案选型优先级

### 18. 优先级顺序：安全性 > 长期稳定性 > 技术成熟度 > 部署便捷度 > 日常使用便捷度

### 19. 版本选择
- 优先官方 LTS；最小权限运行；适配 macOS；拒绝内测/小众实验工具

## 网络资源规范

### 20. 链接要求
- 仅提供可正常访问的有效链接；失效立即替换本地部署或离线源码

## 日常固定服务

### 21. 每日工作
- 整理归档设备环境配置；提供一键备份脚本；定期检测服务/后台/网络/接口状态；阶段完工自动核验文档与真实环境一致性

## 会话复盘经验

### 22. 持续积累
1. **功能边界清晰化**：实验性/废弃代码完全隔离，不接入生产路由；删除废弃模块前先检查所有 `from xxx import` 引用；未实现路由返回明确「开发中」提示
2. **清理策略文档化**：大型清理操作在设计文档记录变更；清理前先做环境备份；磁盘释放数据附备注便于核对

## 回复格式

### 23. 精简回复策略（默认）
- 每次仅发送一条简洁最终回答（1-3 句），避免重复背景/问候；工具输出精炼归纳，禁止全文重复
- 元信息一行：`轮次：N  ▶️ 下一步：<action>`；如需移除须更新本文件并获项目负责人确认
- 审计/复盘场景可临时展开更详尽的 commentary

### 24. 技术方案格式
- 输出技术方案固定双栏：✅可稳定达成的效果 / ⚠️理论最优效果与当前限制

## 附录

### 25. 常用命令
```bash
python3 scripts/check_env.py     # 检查环境
bash scripts/start-feishu.sh     # 启动服务
bash scripts/stop-feishu.sh      # 停止服务
python3 scripts/test_feishu.py   # 运行测试
curl http://127.0.0.1:5103/health  # 检查状态
```

### 26. 文件关系
- `src/feishu_integration.py` 核心实现 · `config/feishu.yaml` 配置 · `scripts/` 脚本集 · `.env_type` 环境标记 · `README.md` 文档

### 27. 常见问题
- 环境验证失败 → `python3 scripts/check_env.py`
- 端口被占用 → 释放 5103
- 模块导入失败 → 确认在项目根运行
- 功能异常 → 检查 `feishu_bot.log`

---

*本文件由 Free API Hub 项目自动生成，持续更新中*
*版本：v1.1 | 更新日期：2026-08-05*
